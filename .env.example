# Copy these keys to .env when local credentials are needed.
# Framework run settings are managed by framework.config.json / Admin Dashboard.

LOGIN_ENABLED=false
LOGIN_USERNAME=dummy-user
LOGIN_PASSWORD=dummy-password
LOGIN_TABEL=0000
