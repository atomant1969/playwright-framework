# Local credentials and sensitive runtime values.
# Run settings such as TEST_SUITE, BASE_URL, workers, retries, and timeouts are managed by framework.config.json / Admin Dashboard.

LOGIN_ENABLED=false
LOGIN_USERNAME=dummy-user
LOGIN_PASSWORD=dummy-password
LOGIN_TABEL=0000
