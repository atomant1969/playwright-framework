name: Playwright Framework Checks

on:
  push:
    branches: [main, canary]
  pull_request:
    branches: [main, canary]
  workflow_dispatch:
    inputs:
      test_suite:
        description: Suite key to run. Use "parallel" to run PARALLEL_SUITE_KEYS as separate suite jobs.
        required: true
        default: smoke
      parallel_suite_keys:
        description: Comma-separated suite keys used when test_suite is "parallel".
        required: false
        default: smoke,api_smoke

jobs:
  select-suites:
    name: select-suites
    timeout-minutes: 15
    runs-on: ubuntu-latest
    outputs:
      suite_matrix: ${{ steps.matrix.outputs.suite_matrix }}
      suite_count: ${{ steps.matrix.outputs.suite_count }}
    env:
      TEST_SUITE: ${{ inputs.test_suite || 'smoke' }}
      PARALLEL_SUITE_KEYS: ${{ inputs.parallel_suite_keys || 'smoke,api_smoke' }}
      HEADLESS: true
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Framework doctor
        run: pnpm doctor
      - name: Typecheck
        run: pnpm typecheck
      - name: Show workflow inputs
        run: |
          echo "TEST_SUITE=$TEST_SUITE"
          echo "PARALLEL_SUITE_KEYS=$PARALLEL_SUITE_KEYS"
      - name: Validate suite registry
        run: pnpm validate:suites
      - name: Check formatting
        run: pnpm format:check
      - name: Lint
        run: pnpm lint
      - name: Generate suite matrix
        id: matrix
        run: pnpm matrix
      - uses: actions/upload-artifact@v4
        if: ${{ success() }}
        with:
          name: suite-matrix
          path: test-results/suite-matrix.*
          if-no-files-found: error
          retention-days: 30

  run-suite:
    name: suite ${{ matrix.order }} - ${{ matrix.suite }} [${{ matrix.mode }}, ${{ matrix.workers }} workers]
    needs: select-suites
    if: ${{ needs.select-suites.outputs.suite_count != '0' }}
    timeout-minutes: 60
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.select-suites.outputs.suite_matrix) }}
    env:
      TEST_SUITE: ${{ matrix.suite }}
      PLAYWRIGHT_WORKERS: ${{ matrix.workers }}
      HEADLESS: true
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Show selected suite
        run: |
          echo "Suite: ${{ matrix.suite }}"
          echo "Kind: ${{ matrix.kind }}"
          echo "Mode: ${{ matrix.mode }}"
          echo "Workers: ${{ matrix.workers }}"
          echo "Description: ${{ matrix.description }}"
      - name: Generate suite test summary
        run: pnpm suite:summary
      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps
      - name: Run suite ${{ matrix.suite }}
        run: pnpm test
      - name: Generate suite result summary
        if: ${{ always() }}
        run: pnpm suite:results
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report-${{ matrix.suite }}
          path: playwright-report/
          retention-days: 30
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: test-results-${{ matrix.suite }}
          path: test-results/
          if-no-files-found: ignore
          retention-days: 30
